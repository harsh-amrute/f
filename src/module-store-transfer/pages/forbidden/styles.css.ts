import { style } from "@vanilla-extract/css";

export const SCPageForbidden = style({
  height: "95%",
  display: "flex",
  justifyContent: "center",
  flexDirection: "column",
  alignItems: "center",
});

export const SCTextTitle = style({
  fontSize: "50px",
});

export const SCTextContent = style({
  fontSize: "30px",
});

export const PermissionForbiddenIcon = style({
  // add sizing if needed later
  // width: '100%',
  // height: '100%',
});
