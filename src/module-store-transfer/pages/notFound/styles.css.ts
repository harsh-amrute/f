import { style } from '@vanilla-extract/css';

export const SCPageNotFound = style({
  height: '85vh',
  display: 'flex',
  justifyContent: 'center',
  flexDirection: 'column',
  alignItems: 'center',
});

export const SCTextTitle = style({
  fontSize: '50px',
});

export const SCTextContent = style({
  fontSize: '30px',
});

export const PageNotFoundIcon = style({
  // align:center;
  // width: 'auto',
  // height: 'auto',
});